<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/main_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoldBlock"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChoiceActivity"></activity>
        <activity android:name=".UserInitActivity"></activity>
        <activity android:name=".UserJoinActivity"></activity>
        <activity android:name=".UserLoginActivity"></activity>
        <activity android:name=".UserMainActivity"></activity>
        <activity android:name=".UserRegistActivity"></activity>
        <activity android:name=".AdminInitActivity"></activity>
        <activity android:name=".AdminLoginActivity"></activity>
        <activity android:name=".AdminJoinActivity"></activity>
        <activity android:name=".AdminMainActivity"></activity>
        <activity android:name=".UserGradeActivity"></activity>
        <activity android:name=".QrDialog" android:theme="@android:style/Theme.Dialog" />
    </application>

</manifest>